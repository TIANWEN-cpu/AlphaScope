# 研策中枢 AlphaScope Core Runtime Dependencies
# Install: pip install -r requirements-core.txt

streamlit==1.57.0
pandas==3.0.3
numpy==2.4.5
plotly==6.7.0
duckdb>=1.0,<2  # DuckDB/Parquet 数据湖 (批量选股/因子扫描); 缺失时数据湖能力优雅降级, 不影响其余功能
exchange_calendars>=4.0  # 真实交易日历 (节假日/周末/T+1); 缺失时回落周末启发式, 不影响其余功能
pandera>=0.20  # 数据契约 schema 校验; 缺失时仅做字段存在性检查, 不影响其余功能
quantstats>=0.0.62  # 专业级绩效报告 (Sharpe/Sortino/Max DD/热力图); 缺失时降级, 不影响其余功能
backtrader>=1.9.78  # 经典事件驱动回测引擎 (多引擎互补); 缺失时降级, 不影响其余功能
lib-pybroker>=1.2.0  # ML + walk-forward 回测引擎 (PyPI 包名 lib-pybroker, import 名 pybroker); 缺失时降级
mcp>=1.0  # Model Context Protocol server (暴露研究工具给外部 LLM); 缺失时 MCP 能力降级, 不影响其余功能
akshare==1.18.60
requests==2.32.3
curl_cffi==0.13.0
openai==2.3.0
python-dotenv==1.1.0
pyyaml==6.0.2
ruamel.yaml>=0.18,<0.19  # 保留注释的 YAML round-trip 编辑 (数据源权重配置); 不可用时回退 PyYAML
pydantic==2.11.3
tenacity==9.1.2
aiohttp==3.12.4
apscheduler==3.11.0
