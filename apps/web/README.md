# AI-Finance Workbench Frontend

Vite + React 前端，默认连接本地 FastAPI：

```bash
npm install
npm run dev
```

默认地址：

- Frontend: http://localhost:3000
- API: http://127.0.0.1:8000

如需指定 API 地址，在 `.env.local` 中设置：

```bash
VITE_API_BASE_URL=http://127.0.0.1:8000
```
