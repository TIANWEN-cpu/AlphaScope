# AI-Finance Python dependencies
# 安装: python -m pip install -r requirements.txt

# 核心运行时
streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# 数据源
akshare>=1.13.0
requests>=2.28.0

# LLM SDK
openai>=1.10.0
python-dotenv>=1.0.0

# v0.7 新增
pyyaml>=6.0          # 解析 config/experts.yaml(5 位专家圆桌人设)

# v0.9 开发依赖(可选,仅在跑 pytest 时需要)
pytest>=7.0          # backend/tests 目录下的纯函数单元测试
