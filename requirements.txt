# AI-Finance Dependencies (v0.40)
# Full install:  pip install -r requirements.txt
# Core only:     pip install -r requirements-core.txt
# API server:    pip install -r requirements-api.txt
# RAG/Vector:    pip install -r requirements-rag.txt
# Development:   pip install -r requirements-dev.txt

-r requirements-core.txt
-r requirements-api.txt
-r requirements-dev.txt

# Optional: uncomment to enable RAG/vector search
# -r requirements-rag.txt

# Optional data sources (install as needed)
# tushare>=1.4.0
# baostock>=0.8.8
# openbb>=4.0.0

# Optional observability
# opentelemetry-api==1.33.0
# opentelemetry-sdk==1.33.0
