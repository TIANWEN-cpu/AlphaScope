# 研策中枢 AlphaScope FastAPI Backend Dependencies
# Install: pip install -r requirements-core.txt -r requirements-api.txt

-r requirements-core.txt
fastapi>=0.115.0,<1.0
uvicorn>=0.34.0,<1.0
python-multipart>=0.0.20  # FastAPI UploadFile 必需
