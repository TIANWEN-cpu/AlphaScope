[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    network: tests that require live network access; excluded in CI via -m "not network"
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers --ignore=tests/probes
