# AI-Finance RAG / Vector Store Dependencies (optional)
# Install: pip install -r requirements-core.txt -r requirements-rag.txt
# Note: chromadb requires C++ Build Tools on Windows.
# If installation fails, RAG features will be disabled gracefully.

chromadb==0.6.3
